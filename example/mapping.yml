files:
  example: "./example.json"

from_env:
  BQ_LOC: DESTINATION__BIGQUERY__LOCATION

example:
  GITHUB_ACCESS_TOKEN: "DESTINATION__BIGQUERY__CREDENTIALS__PROJECT_ID"
  OPENROUTER_API_KEY: "OPENROUTER_API_KEY"

